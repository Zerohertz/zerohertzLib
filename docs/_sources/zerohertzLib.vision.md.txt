# zerohertzLib.vision

```{important}

- `cwh`: `[cx, cy, w, h]` 로 구성된 bbox (`[4]` or `[N, 4]`)
- `xyxy`: `[x0, y0, y1, y2]` 로 구성된 bbox (`[4]` or `[N, 4]`)
- `poly`: `[[x0, y0], [x1, y1], [x2, y2], [x3, y3]]` 로 구성된 bbox (`[4, 2]` or `[N, 4, 2]`)
```

```{eval-rst}
.. automodule:: zerohertzLib.vision
    :members:
    :undoc-members:
    :show-inheritance:
```
